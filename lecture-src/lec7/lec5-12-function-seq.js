// 먼저 형식을 만든다.
function add() {}

// 함수에 인자가 필요하다면, 인자를 기술한다.
function add(a, b) {}

// 꺽쇠에 엔터를 쳐서 함수 내용을 기술할 줄을 만든다.
function add(a, b) {

}

// 내용을 작성한다.
function addNumbers(a, b) {
  return a + b;
}
